@using Profais.Services.ViewModels.Shared;
@using Profais.Services.ViewModels.Task;
@model PagedResult<RecoverTaskViewModel>

@{
    ViewData["Title"] = "Recovery Panel - View deleted tasks";

    Model.PaginationViewModel.Area = "Admin";
    Model.PaginationViewModel.Controller = "RecoveryPanel";
    Model.PaginationViewModel.Action = "ViewDeletedTasks";
}

<div class="container mt-4">
    <h2>
        @ViewData["Title"]
    </h2>

    <div class="row">
        @foreach (RecoverTaskViewModel task in Model.Items)
        {
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title" style="color:black">
                            @task.Title
                        </h5>

                        <p class="card-text" style="color:black">
                            @task.Id
                        </p>

                        <p class="card-text" style="color:black">
                            @task.Description
                        </p>

                        <p class="card-text" style="color:black">
                            @task.ProjectId
                        </p>

                        <form asp-area="Admin" asp-action="RecoverTask" asp-controller="RecoveryPanel" method="post">
                            <input value="@task.Id" hidden name="id" />

                            <button type="submit" class="btn btn-warning" style="color:black">
                                Recover Task
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>

    <partial name="_Pagination" model="Model.PaginationViewModel" />
</div>