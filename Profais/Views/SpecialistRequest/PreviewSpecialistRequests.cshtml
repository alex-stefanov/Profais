@using Profais.Services.ViewModels.SpecialistRequest;
@model ICollection<SpecialistRequestViewModel>;

@{
    ViewData["Title"] = "Specialist Requests - Preview";
}

<div class="container mt-4">
    <h2>
        @ViewData["Title"]
    </h2>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Client Id
                </th>

                <th>
                    First Name
                </th>

                <th>
                    Last Name
                </th>

                <th>
                    Profix Id
                </th>

                <th>
                    Current Status
                </th>

                <th>
                    Actions
                </th>
            </tr>
        </thead>

        <tbody>
            @foreach (SpecialistRequestViewModel request in Model)
            {
                <tr>
                    <td>
                        @request.UserId
                    </td>

                    <td>
                        @request.FirstName
                    </td>

                    <td>
                        @request.LastName
                    </td>

                    <td>
                        @request.ProfixId
                    </td>

                    <td>
                        @request.Status.ToString()
                    </td>

                    <td>
                        <form asp-action="ApproveSpecialistRequest" asp-controller="SpecialistRequest" method="post">
                            <input value="@request.Id" hidden name="model.Id" />
                            <input value="@request.UserId" hidden name="model.UserId" />

                            <button type="submit" class="btn btn-success">
                                Approve
                            </button>
                        </form>

                        <form asp-action="DeclineSpecialistRequest" asp-controller="SpecialistRequest" method="post">
                            <input value="@request.Id" hidden name="model.Id" />
                            <input value="@request.UserId" hidden name="model.UserId" />

                            <button type="submit" class="btn btn-danger">
                                Decline
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>