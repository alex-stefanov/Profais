@using Profais.Services.ViewModels.Shared;
@using Profais.Services.ViewModels.ProjectRequest;
@model PagedResult<CollectionProjectRequestViewModel>

@{
    ViewData["Title"] = "Project Request - Declined";

    Model.PaginationViewModel.Area = "";
    Model.PaginationViewModel.Controller = "ProjectRequest";
    Model.PaginationViewModel.Action = "ViewDeclinedProjectRequests";
}

<div class="container mt-4">
    <h2>
        @ViewData["Title"]
    </h2>

    <div class="row">
        @foreach (CollectionProjectRequestViewModel project in Model.Items)
        {
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            @project.Title
                        </h5>

                        <p class="card-text">
                            @project.ClientName
                        </p>

                        <form asp-action="ViewProjectRequest" asp-controller="ProjectRequest" method="get">
                            <input value="@project.Id" hidden name="projectRequestId" />

                            <button type="submit" class="btn btn-primary">
                                View Project
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>

    <partial name="_Pagination" model="Model.PaginationViewModel" />
</div>